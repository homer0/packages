/**
 * The default name for a config when none is provided.
 */
export const DEFAULT_CONFIG_STORE = 'main';
/**
 * The ID of the script tag that will be rendered in the page.
 * The `{name}` placeholder will be replaced with the actual name of the config.
 */
export const SCRIPT_CONFIG_ID = '__NEXT_SSR_CONFIG-{name}__';
